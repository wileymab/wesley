{
  "name": "wesley",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:common": "babel ./common/src -d ./common/dist",
    "build:localService": "babel ./local/src -d ./local/dist",
    "build:firebase": "babel ./firebase/src -d ./firebase/dist",
    "build:all": "yarn build:common && yarn build:localService && yarn build:firebase"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-flow": "^6.23.0",
    "flow-bin": "^0.69.0"
  }
}
